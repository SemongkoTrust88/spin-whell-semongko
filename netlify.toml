[build]
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# This project is a static frontend. The real /wheel/start/* endpoint
# must be provided by your backend/API server.
